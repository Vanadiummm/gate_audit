"""proxy 包：代理核心。

职责分工（读代码建议按这个顺序）：
- context.py    ：一次运行里各模块共享的依赖集合（配置/引擎/存储/日志/角色）。
- httpmsg.py    ：HTTP 首部的读取与解析、消息体搬运等"协议通用工具"。
- forward.py    ：普通 HTTP 请求的转发（含黑白名单判定 + 审计）。
- connect.py    ：HTTPS 的 CONNECT 隧道（只判域名，不解密内容）。
- connection.py ：对每个客户端连接做"首行分派"。
- main.py       ：把上面这些装配起来并启动（含 Flask 管理端线程）。

整体数据流（本项目的核心思路，务必记住）：
    客户端 -> [读首部] -> [查规则：转发之前、内联完成 = 快路径]
                     -> 命中拦截：直接 403，记审计
                     -> 放行    ：连上游、转发、记审计（记审计是异步的 = 慢路径）
"""
